---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="404 - Page Not Found">
  <div class="flex flex-col items-center justify-center min-h-[60vh] text-center">
    <h1 class="text-8xl font-bold text-gray-300 dark:text-gray-700">404</h1>
    <p class="text-xl text-gray-600 dark:text-gray-400 mt-4">
      Oops! The page you're looking for doesn't exist.
    </p>
    <a
      href="/"
      class="mt-8 px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
    >
      Go Home
    </a>
  </div>

  <!-- White Moonlight - 白月光 -->
  <div id="moonlight" class="fixed top-8 right-8 cursor-pointer select-none transition-all duration-1000 hover:scale-110">
    <div class="moon-glow"></div>
    <div class="moon"></div>
  </div>

  <style>
    .moon {
      width: 32px;
      height: 32px;
      background: radial-gradient(circle at 30% 30%, #fffef0 0%, #f5f5dc 50%, #e8e4d4 100%);
      border-radius: 50%;
      box-shadow:
        0 0 20px rgba(255, 255, 240, 0.4),
        0 0 40px rgba(255, 255, 240, 0.2),
        0 0 60px rgba(255, 255, 240, 0.1);
      position: relative;
      z-index: 2;
      animation: moonFloat 6s ease-in-out infinite;
    }

    .moon-glow {
      position: absolute;
      width: 60px;
      height: 60px;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: radial-gradient(circle, rgba(255, 255, 240, 0.15) 0%, transparent 70%);
      border-radius: 50%;
      z-index: 1;
      animation: glowPulse 4s ease-in-out infinite;
    }

    @keyframes moonFloat {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-5px); }
    }

    @keyframes glowPulse {
      0%, 100% { opacity: 0.6; transform: translate(-50%, -50%) scale(1); }
      50% { opacity: 1; transform: translate(-50%, -50%) scale(1.1); }
    }

    :global(.dark) .moon {
      box-shadow:
        0 0 30px rgba(255, 255, 240, 0.5),
        0 0 60px rgba(255, 255, 240, 0.3),
        0 0 90px rgba(255, 255, 240, 0.15);
    }

    :global(.dark) .moon-glow {
      background: radial-gradient(circle, rgba(255, 255, 240, 0.25) 0%, transparent 70%);
    }
  </style>

  <script>
    const moon = document.getElementById('moonlight');

    moon?.addEventListener('click', () => {
      // Trigger the firework easter egg
      document.dispatchEvent(new KeyboardEvent('keydown', { key: 'a' }));
      document.dispatchEvent(new KeyboardEvent('keydown', { key: 'o' }));
      document.dispatchEvent(new KeyboardEvent('keydown', { key: 'q' }));
      document.dispatchEvent(new KeyboardEvent('keydown', { key: 'i' }));
    });
  </script>
</BaseLayout>
